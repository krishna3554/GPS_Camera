name: gps_camera_app
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  camera: ^0.11.1
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  sqflite: ^2.3.3
  path_provider: ^2.1.4
  path: ^1.9.0
  gallery_saver_plus: ^3.2.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  share_plus: ^10.0.2
  permission_handler: ^11.3.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
